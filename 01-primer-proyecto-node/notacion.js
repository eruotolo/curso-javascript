// NOTACIONES

// ; --> El punto y coma se utiliza para delimitar el final de una línea
const b = 4;
console.log(b);

// . --> El punto se utiliza en los objetos, para acceder a los atributos
// movil.ancho

// [] --> Paréntesis recto, se utilizar para las listas, arreglos o arrays.
const arr = [1, 2, 3];
console.log(arr[1]);

// () --> Paréntesis curvos, se utiliza para las funciones
function suma(a, b) {
    // se escribe la función
}

// {} --> Las llaves {} se utilizan para objetos, funciones y estructuras de control.
const movil = {
    ancho: 5,
    altura: 10,
}

if (true) {
    // todo lo que haya entre llaves
    const constante2 = "hola";
}
